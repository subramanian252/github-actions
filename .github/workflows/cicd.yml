name: CI/CD Pipeline

on:
  push:
    branches:
      - main


jobs:
  job1:
    name: Hello World
    runs-on: windows-latest
    steps:
      - name: echo name
        shell: pwsh
        run: get-computerinfo


