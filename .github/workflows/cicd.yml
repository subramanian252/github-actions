name: CI/CD Pipeline

on:
  schedule:
    - cron: '5 * * * *' 
  workflow_dispatch:


jobs:
  job1:
    name: Hello World
    runs-on: windows-latest
    steps:
      - name: get computer info
        shell: pwsh
        run: get-computerinfo


